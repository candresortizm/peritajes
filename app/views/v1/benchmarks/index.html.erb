<h1>Benchmarks</h1>
<%@all.each do |element|%>
<p><%=element%></p>
<%end%>

<%if current_user.type.eql?("CarExpert")%>
  <%= link_to('New Benchmark', new_benchmark_path, method: :get) %>
<%end%>
<li>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
</li>
