var current_tab = $(".tab-pane.active");
current_tab.empty();
current_tab.append("<%= j(render 'car_processes_table') %>");
if(current_tab.attr('id') != "<%=@process_type%>"){
  $(".nav-tabs .nav-link").toggleClass("active");
}
